export interface IBenefit {
    title: string,
    icon: string,
    id: string,
}

export interface IHeadline {
    title: string
    description?: string
    line?: boolean
    lineOrange?: boolean
}